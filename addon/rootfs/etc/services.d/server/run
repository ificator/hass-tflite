#!/usr/bin/with-contenv bashio
set -euo pipefail

export PORT=8000
export MODEL_DIR=/config/models
export STATIC_DIR=/opt/app/static

mkdir -p "${MODEL_DIR}" || true
chmod 0775 "${MODEL_DIR}" || true

exec /usr/local/bin/tflite-server
