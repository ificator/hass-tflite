#!/usr/bin/with-contenv bashio
set -euo pipefail

export PORT=8000
export MODEL_DIR=/data/models

mkdir -p /data/models || true
chmod 0775 /data/models || true

cd /opt/app
exec uvicorn main:app --host 0.0.0.0 --port "${PORT}"
